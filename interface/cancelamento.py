import customtkinter as ctk
from interface._base import _topbar_base, _placeholder


class CancelamentoContent:

    def __init__(self, parent):
        _topbar_base(parent, "Cancelamento")
        _placeholder(parent, "Cancelamento")
